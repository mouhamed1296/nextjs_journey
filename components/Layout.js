const Layout = ({children}) => {
    return (
        <div>
            <main>
                <h1>Hello World</h1>
                {children}
            </main>
        </div>
    )
}

export default Layout
